<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.home.categoriesprodcut">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Categories">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity android:name=".Categories" /> -->
        <!-- <activity android:name=".CategoriesAdapter"></activity> -->
        <!-- <activity android:name=".ProductsActivity"></activity> -->
        <activity android:name=".ProductDetailActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".ProductsActivity"/>
        <!--<activity android:name=".ProductImages" />-->
        <!--<activity android:name=".ProductImagesAdapter"></activity>-->
    </application>

</manifest>